app: 
  fullName: application-backend-staging
  name: application
  type: backend
  env: staging

container:
  port: 8080
  image:
    registry: registry.hub.docker.com
    version: latest

ingress:
  prefix: /applications


host: staging-api.xquare.app

