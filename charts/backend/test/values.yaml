app: 
  fullName: test-backend-staging
  name: test
  type: backend
  env: staging

container:
  port: 8080
  image:
    registry: registry.hub.docker.com
    version: latest

ingress:
  prefix: /test


host: staging-api.xquare.app

