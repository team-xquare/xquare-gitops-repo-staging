app: 
  fullName: application-be-stag
  name: application
  type: be
  env: stag

container:
  port: 8080
  image:
    registry: registry.hub.docker.com
    version: latest

ingress:
  prefix: /applications


host: stag-api.xquare.app

