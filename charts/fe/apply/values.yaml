app: 
  fullName: apply-fe-stag
  name: apply
  type: fe
  env: stag

container:
  port: 3000
  image:
    registry: registry.hub.docker.com
    version: latest

ingress:
  prefix: /apply


host: stag-service.xquare.app

